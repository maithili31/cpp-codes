#include <bits/stdc++.h>
using namespace std;

int main () {
    int i=0;
    i++;
    if (i<=5) {
        printf ("c adds wings to your thoughts\n");
        exit (0);
        main ();
    }
    return 0;
}